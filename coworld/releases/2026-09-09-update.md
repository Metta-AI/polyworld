# Polyworld Softmax update

All three Coworlds are canonical at version `2026.9.9.4`, built from
Polyworld commit `d8960441d675caa5e403154ebc30487108e8be32` on `master`.
Native games and browser replay viewers use the same source and dependency
locks. This update includes the navigation fix, shared UI scaling, backgrounds,
MSAA with separate shadow passes, and default replay looping.

| Game | League | New Coworld |
| --- | --- | --- |
| Gods of the Arena | [Play](https://softmax.com/gods-of-the-arena) | `cow_eca75986-4007-4dc7-be31-8f1f40ea2287` |
| Light vs Dark | [Play](https://softmax.com/light-vs-dark) | `cow_a15fa579-49fb-4427-ab3f-7d8c488dda81` |
| Call to Adventure | [Play](https://softmax.com/call-to-adventure) | `cow_2e4565e4-0673-4d0e-99fc-6861be7f4c51` |

All three local certifications and all three hosted certifications passed their
ten checks. All 15 hosted upload smoke episodes completed successfully.
`nim check tests/tests.nim` and `nim r tests/tests.nim` passed.

Each existing league automatically adopted the new canonical release. Round 9
completed in every league with two episodes and published Elo standings.
Both explicit-roster and league-targeted experience requests completed for each
game. The 12 acceptance episodes have valid binary results, replays, game logs,
and all 64 player logs. No episode, compilation, or VM runtime errors were found.
Filler flags match the configured baseline policies.

All three league pages and all six experience replay pages rendered their new
WASM viewers in the browser with looping enabled. The local test server was
stopped before hosted replay verification. Initial GotA and LvD asset downloads
emitted Emscripten dependency-wait diagnostics and then loaded successfully.
The hosted checks verified startup and ongoing playback. Full replay determinism
and navigation regressions were verified during the preceding navigation fix.

Existing players, submissions, and filler policies were preserved. Each league
now has two active champions. Scheduling remains enabled at 30-minute intervals.
Ordinary local WASM builds were restored after building the replay packages.

Exact source and asset revisions, lock and bundle hashes, certification jobs,
experience requests, rounds, replay links, and artifact checks are in
[the deployment receipt](2026-09-09-update.json).
