# GotA Softmax update

GotA version `2026.9.14.1` is canonical at
[softmax.com/gods-of-the-arena](https://softmax.com/gods-of-the-arena).
The game server and browser replay viewer were built from
`f346f932803787ac3cb218a85394b62969e9098f`.

Hosted matches use the saved seed 54 map preset, including camp size 36,
32 jungle roads, and camps touching roads. Replays store their map presets.
The browser bundle includes the updated terrain and smaller trees.

Local Docker certification and hosted certification each passed all ten checks.
All five hosted smoke matches passed. The separate local Softmax stack was not
run because its inactive development environment required a full toolchain
rebuild. Browser playback passed locally and on the public site.

The requested immediate round, round 256, completed all twelve matches on the
new release. Its saved game configuration matches the chosen map preset, and
the public game page plays its replays with the new viewer. Existing league
entrants, filler policies, and scheduling settings were preserved.

Release identifiers, hashes, map settings, and verification evidence are in
[the deployment receipt](2026-09-14-gota.json).
